{
  "title": "Exclusions",
  "$schema": "exclusions.json",
  "type": "array",
  "format": "table",
  "options": {
    "disable_collapse": true
  },
  "items": {
    "title": "Exclusion",
    "$ref": "#/definitions/exclusionField"
  },
  "definitions": {
    "exclusionField": {
      "title": "Exclusion",
      "headerTemplate": "{{ self.key }}",
      "type": "object",
      "options": {
        "collapsed": false,
        "disable_collapse": true
      },
      "properties": {
        "value": {
          "propertyOrder": 10,
          "type": "string",
          "format": "date",
          "title": "Date",
          "options": {
            "infoText": "The date to exclude. Can be in the format of 01/31/2020 or 01/31/yyyy (for annually repeating dates)."
          },
          "default": "",
          "minLength": 5,
          "maxLength": 10
        },
        "description": {
          "propertyOrder": 20,
          "type": "string",
          "title": "Description",
          "options": {
            "infoText": ""
          },
          "default": "",
          "minLength": 0,
          "maxLength": 255
        }
      }
    }
  }
}